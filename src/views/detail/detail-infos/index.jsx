import React, { memo } from "react";
import { InfosWrapper } from "./style";

const DetailInfos = memo(() => {
  return <InfosWrapper>抱歉,没有后台数据,所以暂时没做</InfosWrapper>;
});

export default DetailInfos;
